# 한발짝 (hanbaljjak.com) — 구축 명세

> 이 문서는 Claude Code가 읽고 그대로 구현하는 사양서입니다.
> 임의로 범위를 넓히지 말고, Phase 순서대로만 진행하십시오.
> 각 Phase 끝의 **완료 조건**을 실제로 검증한 뒤 다음으로 넘어갑니다.

---

## 0. 프로젝트 개요

| 항목 | 값 |
|---|---|
| 사이트 이름 | 한발짝 |
| 도메인 | hanbaljjak.com |
| 슬로건 | 한 발짝씩 나아지는 몸 |
| 주제 | 40~60대 근골격계 건강 정보 |
| 저자 | 허지원. 물리치료사 (경력 10년), 가정의학과 의원 근무 |
| 목적 | 검색 유입 기반 정보 사이트 + 애드센스 |
| 대상 독자 | 40~60대. 모바일 이용 비중 높음. 의학 용어에 익숙하지 않음 |

### 성격 규정

의료 행위를 하는 사이트가 아니라 **건강 정보 사이트**입니다. 다음을 반드시 지킵니다.

- 진단·처방·치료를 표방하지 않습니다
- 특정 의료기관, 제품, 시술을 홍보하지 않습니다
- 모든 글에 면책 문구와 적색 신호(응급 징후) 안내가 들어갑니다
- 효과를 단정하지 않습니다 ("낫습니다" ✗ / "도움이 되는 경우가 많습니다" ✓)

---

## 1. 기술 스택

| 영역 | 선택 |
|---|---|
| 프레임워크 | Next.js (App Router) |
| 렌더링 | 전 페이지 빌드 타임 정적 생성 (SSG) |
| 스타일 | Tailwind CSS |
| 콘텐츠 | 마크다운 파일 + frontmatter |
| 폰트 | Pretendard 로컬 서빙 + 시스템 폰트 폴백 |
| 배포 | Cloudflare Pages |
| 분석 | Google Analytics 4 + Google Search Console |

### 구현 원칙

- 홈, 글 목록, 글 상세, 카테고리, 정적 페이지는 **모두 `generateStaticParams`로 빌드 타임 생성**합니다. 런타임 서버 렌더링에 의존하지 않습니다.
- `'use client'`는 모바일 메뉴, 목차 스크롤 하이라이트 등 실제 인터랙션이 필요한 곳에만 씁니다.
- 이미지는 `next/image`, 폰트는 `next/font/local`로 처리합니다.
- 외부 폰트 CDN 요청을 만들지 않습니다.
- Cloudflare Pages 배포를 위해 `output: 'export'` 정적 내보내기 설정을 사용합니다.

---

## 2. 디렉토리 구조

```
/
├── app/
│   ├── layout.tsx
│   ├── page.tsx                    홈
│   ├── posts/[slug]/page.tsx       글 상세
│   ├── category/[category]/page.tsx 카테고리 목록
│   ├── about/page.tsx              소개
│   ├── privacy/page.tsx            개인정보처리방침
│   ├── disclaimer/page.tsx         면책조항
│   ├── contact/page.tsx            문의
│   ├── sitemap.ts
│   └── robots.ts
├── components/
│   ├── Header.tsx
│   ├── Footer.tsx
│   ├── PostCard.tsx
│   ├── TableOfContents.tsx
│   ├── AuthorBox.tsx
│   ├── Disclaimer.tsx
│   └── RedFlagBox.tsx
├── content/
│   └── posts/*.md
├── lib/
│   ├── posts.ts                    마크다운 파싱
│   └── categories.ts
├── public/
│   ├── images/posts/
│   └── fonts/
└── SPEC.md
```

---

## 3. 카테고리

**5개**로 운영합니다. 임의로 늘리거나 줄이지 마십시오.

| slug | 이름 | 포함 부위 |
|---|---|---|
| `back` | 허리·목 | 요통, 추간판장애, 척추관협착증, 경추 |
| `shoulder` | 어깨·팔 | 오십견, 회전근개, 상과염, 손목, 손가락 |
| `knee` | 무릎·발 | 무릎관절증, 반월판, 족저근막염 |
| `recovery` | 수술 후 재활 | 인공관절, 봉합술, 척추수술 이후 |
| `body` | 몸 전체 | 골다공증, 근감소증, 낙상, 자세, 통계 |

`recovery`는 이 사이트의 핵심 클러스터입니다. 저자의 임상 경험이 가장 직접적으로 반영되는 영역이므로 부속 카테고리로 취급하지 마십시오.

카테고리별 목표 편수와 글 목록은 `CONTENT-PLAN.md`를 따릅니다.

## 4. URL 설계

| 페이지 | 경로 |
|---|---|
| 홈 | `/` |
| 글 상세 | `/posts/[slug]` |
| 카테고리 | `/category/[category]` |
| 소개 | `/about` |
| 개인정보처리방침 | `/privacy` |
| 면책조항 | `/disclaimer` |
| 문의 | `/contact` |

slug는 영문 소문자와 하이픈만 사용합니다. 예: `disc-vs-stenosis`, `plantar-fasciitis-morning-pain`

---

## 5. 프론트매터 스키마

```yaml
---
title: "허리디스크와 척추관협착증, 뭐가 다른가요"
description: "앉으면 아픈지 걸으면 아픈지에 따라 달라집니다. 두 질환의 차이와 진료 시 전달할 내용을 정리했습니다."
slug: "disc-vs-stenosis"
category: "back"
tags: ["척추관협착증", "허리디스크", "요통"]
publishedAt: "2026-08-15"
updatedAt: "2026-08-15"
coverImage: "/images/posts/disc-vs-stenosis/cover.png"
readingTime: 8
references:
  - label: "Brinjikji W, et al. AJNR. 2015;36(4):811-816."
    url: "https://pubmed.ncbi.nlm.nih.gov/25430861/"
---
```

- `description`은 검색 결과에 노출되므로 80~120자로 작성합니다.
- `references`가 있으면 글 하단에 참고문헌 목록을 자동 렌더링합니다.

---

## 6. 디자인 요구사항

독자가 40~60대이므로 **가독성이 최우선**입니다. 심미성보다 읽기 쉬움을 택합니다.

### 참고 방향

`joshwcomeau.com`의 **여백 감각과 읽기 경험**을 참고합니다. 다만 다음은 가져오지 않습니다.

- 스크롤 애니메이션, 인터랙티브 데모, 마이크로 인터랙션
- 그 사이트의 본문 글자 크기 (개발자 대상이라 우리 기준보다 작습니다)

**가져올 것:** 넉넉한 여백, 본문 폭 제한, 부드러운 색 대비, 따라다니는 목차, 방해되지 않는 위치의 토글.

인터랙션에 시간을 쓰지 않습니다. 이 사이트의 콘텐츠는 글이며, 화면 효과가 아닙니다.

### 수치 기준

| 항목 | 값 |
|---|---|
| 본문 글자 크기 | 모바일 17px, 데스크톱 18px |
| 본문 행간 | 1.8 |
| 본문 최대 너비 | 680px |
| 문단 간격 | 1.4em |
| 최소 터치 영역 | 44×44px |
| 본문 색 | 순수 검정 금지. #1F2937 계열 |
| 배경 | 순백 금지. 아주 옅은 회색 계열 |
| 본문 정렬 | **왼쪽 정렬 고정.** 가운데 정렬·양쪽 정렬 사용 금지 |

- 본문, 소제목, 목록 모두 왼쪽 정렬합니다. 가운데 정렬은 줄 시작점이 흔들려 읽는 속도를 떨어뜨립니다. 양쪽 정렬(justify)은 한글에서 단어 간격이 벌어져 가독성을 해칩니다.
- 회색 위 회색 텍스트 같은 저대비 조합을 쓰지 않습니다. 본문 대비는 WCAG AA 이상을 만족해야 합니다.
- 표는 모바일에서 가로 스크롤되도록 감싸되, 스크롤 가능함을 시각적으로 알 수 있게 합니다.
- 다크모드는 초기 범위에서 제외합니다.

### 구현 방법

`@tailwindcss/typography`를 사용하되, `prose` 기본값을 위 수치로 오버라이드합니다. 처음부터 직접 스타일을 짜지 않습니다.

---

## 6-1. 홈 화면 구성

Phase 2에서 구현합니다. 위에서 아래로 다음 순서입니다.

```
헤더 (로고 + 카테고리 메뉴)
─────────────────────────
사이트 한 줄 소개 + 저자 소개
─────────────────────────
카테고리 칩 5개
─────────────────────────
최근 글 카드 목록
─────────────────────────
먼저 읽어보시면 좋은 글 (아코디언)
─────────────────────────
푸터 (필수 페이지 4개 링크)
```

### 저자 소개 블록

**첫 화면에 노출되어야 합니다.** 건강 정보 사이트에서 저자가 누구인지 즉시 보이는 것은 신뢰도와 직결되며, 애드센스 정책 측면에서도 유리합니다.

- 이름, 직함, 경력 연차, 근무 형태를 한두 줄로
- `/about` 페이지로 링크
- 예: `물리치료사 허지원 · 경력 10년 · 가정의학과 외래 근무`

### 카테고리 칩

5개 카테고리를 칩(pill) 형태로 나열합니다.

**반드시 실제 링크(`<Link href="/category/[slug]">`)로 구현합니다.** 클라이언트 사이드 필터링으로 만들면 카테고리 페이지가 생성되지 않아 검색에 노출되지 않으며, 클러스터 구조가 검색엔진에 전달되지 않습니다. 이는 이 사이트의 SEO 전략 전체를 무력화합니다.

정적 생성이므로 페이지 이동 속도는 필터링과 차이가 없습니다.

### 글 카드

카드 하나에 다음을 표시합니다.

| 요소 | 이유 |
|---|---|
| 제목 | |
| 카테고리 | 어느 부위 글인지 |
| **description 한 줄** | **독자가 자기 증상인지 판단하는 근거** |
| 읽기 시간 | |
| 발행일 | |

**description을 반드시 노출합니다.** 제목만으로는 독자가 자신의 증상에 해당하는 글인지 알기 어렵습니다. 예를 들어 "족저근막염"이라는 제목만 보고는 아침에 발이 아픈 자신의 문제인지 알 수 없습니다.

### 먼저 읽어보시면 좋은 글

아코디언 형태로, 클릭하면 펼쳐지는 목록입니다.

- **초기에는 각 클러스터의 기둥글 5개**를 넣습니다
- 방문자 데이터가 쌓이기 전이므로 "인기 글"이라는 표현은 사용하지 않습니다
- GA 데이터가 3개월 이상 쌓인 뒤 실제 조회수 기준으로 교체합니다

기둥글이 아직 작성되지 않은 초기에는 발행된 글 중 대표성이 있는 것으로 채웁니다.

### 푸터

필수 페이지 4개(`/about`, `/privacy`, `/disclaimer`, `/contact`) 링크를 모두 노출합니다.

**페이지를 만들어두고 링크를 걸지 않으면 애드센스 심사에서 탈락 사유가 됩니다.** 심사자가 접근할 수 있어야 합니다.

---

## 6-2. 광고 슬롯

Phase 6에서 실제 코드를 넣되, **레이아웃 자리는 Phase 2에서 미리 확보**합니다. 나중에 레이아웃을 다시 손대는 것보다 처음부터 공간을 비워두는 편이 낫습니다.

| 위치 | 비고 |
|---|---|
| 첫 h2 위 (본문 시작 전) | |
| 본문 중간 1곳 | 글 길이의 절반 지점 |
| 글 끝 (관련 글 위) | |

**세 자리를 넘기지 않습니다.** 광고가 많으면 이탈률이 오르고 정책 위반 소지가 생깁니다.

- 광고와 본문이 시각적으로 구분되어야 합니다. 콘텐츠처럼 보이게 배치하면 정책 위반입니다
- 광고 영역에 최소 높이를 지정해 레이아웃 이동(CLS)을 방지합니다
- 적색 신호 박스, 면책 문구, 저자 박스 바로 옆에는 광고를 배치하지 않습니다

## 7. 글 페이지 필수 구성 요소

모든 글 상세 페이지는 다음을 포함합니다.

1. 제목, 발행일, 수정일, 예상 읽기 시간
2. 목차 (h2 기준 자동 생성)
3. 본문
4. **적색 신호 박스** — `RedFlagBox` 컴포넌트. 응급 징후 안내
5. 참고문헌 (frontmatter에 있을 경우)
6. **면책 문구** — `Disclaimer` 컴포넌트. 전 글 공통
7. **저자 박스** — `AuthorBox` 컴포넌트. 이름, 면허, 근무 형태
8. 같은 카테고리 관련 글 3개

4번과 6번, 7번은 **모든 글에 예외 없이** 들어갑니다. 이것이 YMYL 주제에서 신뢰도를 만드는 핵심 요소입니다.

---

## 8. SEO 요구사항

- 각 페이지에 고유한 `<title>`, `<meta description>` 생성
- Open Graph, Twitter Card 메타 태그
- **JSON-LD 구조화 데이터**
  - 글 페이지: `Article` + `author`(Person, 직함 포함)
  - 사이트 전체: `WebSite`, `Organization`
  - 해당되는 글: `FAQPage`
- `sitemap.xml` 자동 생성
- `robots.txt` — 전체 허용, sitemap 경로 명시
- canonical URL 지정
- 이미지 alt 속성 필수
- 헤딩 위계 준수 (h1은 페이지당 1개)

---

## 9. Phase 구분

한 번에 하나씩만 진행합니다. 각 Phase 완료 조건을 실제로 확인한 뒤 다음으로 갑니다.

### Phase 1 — 기반

- Next.js 프로젝트 초기화, Tailwind 설정
- Pretendard 로컬 폰트 설정
- 레이아웃, Header, Footer
- 마크다운 파싱 로직 (`lib/posts.ts`)
- 홈, 글 상세, 카테고리 페이지 기본 구조

**완료 조건:** 샘플 마크다운 1개가 `/posts/[slug]`에서 정상 렌더링되고, `npm run build`가 오류 없이 통과한다.

### Phase 2 — 콘텐츠 표시 완성

- 목차, AuthorBox, Disclaimer, RedFlagBox 컴포넌트
- 참고문헌 렌더링
- 관련 글 3개
- 표·이미지 스타일링
- **홈 화면 구성** (6-1장) — 저자 소개, 카테고리 칩, 글 카드, 아코디언, 푸터
- **광고 슬롯 자리 확보** (6-2장) — 코드는 넣지 않고 레이아웃 공간만
- 모바일 반응형 검증

**완료 조건:** 실제 글 2편이 모든 구성 요소를 갖춘 상태로 렌더링된다. 모바일 375px 폭에서 가로 스크롤이 발생하지 않는다. 홈에서 카테고리 칩을 누르면 `/category/[slug]`로 실제 이동한다.

### Phase 3 — 정적 페이지

- 소개, 개인정보처리방침, 면책조항, 문의 페이지
- Footer에서 4개 모두 링크

**완료 조건:** 4개 페이지 모두 접근 가능하고 Footer에 링크가 있다.

### Phase 4 — SEO

- 메타 태그, OG 태그
- JSON-LD
- sitemap.ts, robots.ts
- canonical

**완료 조건:** 빌드 결과물에서 `sitemap.xml`이 생성되고 모든 글 URL이 포함된다. 글 페이지 HTML에 JSON-LD가 들어 있다.

### Phase 5 — 배포

- Cloudflare Pages 연결
- 도메인 연결, HTTPS 확인
- GA4, Search Console 연동
- 사이트맵 제출

**완료 조건:** hanbaljjak.com이 HTTPS로 열리고, Search Console에 사이트맵이 제출된 상태다.

### Phase 6 — 애드센스 (글 20편 이후)

- ads.txt
- 광고 슬롯 배치

**주의:** 글이 20편 미만이면 이 Phase를 시작하지 않습니다.

---

## 10. 콘텐츠 작성 규칙

### 제목

**형식:** `핵심 키워드, 증상 표현`

키워드를 맨 앞에 둡니다. 독자는 질환명을 모르고 증상으로 검색하므로, 뒤에 증상 표현을 붙여 양쪽을 모두 잡습니다.

- 좋음: `족저근막염, 아침 첫걸음이 유독 아픈 이유`
- 나쁨: `족저근막염의 원인과 치료법 총정리`
- 나쁨: `아침에 발이 아파요`

**같은 클러스터 안에서 시작 키워드가 겹치면 안 됩니다.** 어깨 클러스터 글이 모두 "오십견"으로 시작하면 서로 순위를 갉아먹습니다. 두 번째 글부터는 다른 키워드로 시작합니다.

| 순서 | 시작 키워드 |
|---|---|
| 1편 | 오십견 |
| 2편 | 어깨 통증 시기별 |
| 3편 | 회전근개 |
| 4편 | 방아쇠수지 |

### 도입부

**첫 2~3줄에 핵심 키워드가 들어가야 하며, 이 글에서 무엇을 다루는지 명시합니다.**

이 문단이 frontmatter의 `description`이 되고 검색 결과에 노출됩니다. 80~120자로 정리합니다.

### 분량

**3,000~4,000자.**

건강 주제는 깊이가 순위를 만듭니다. 1,500~2,000자로는 표면만 훑게 되며, 그런 글은 이미 포화 상태입니다. 근거와 임상 관점을 담으려면 이 정도 분량이 필요합니다.

대신 읽기 부담을 줄입니다. 한 문단 3~4줄, h2로 자주 끊기, 표와 목록 적극 활용.

### 헤딩 위계

**마크다운 본문은 h2부터 시작합니다.** h1은 마크다운에 쓰지 않습니다.

글 제목(h1)은 frontmatter의 `title`을 글 상세 컴포넌트가 렌더링합니다. 마크다운에 `#`를 쓰면 h1이 두 개가 되어 SEO상 문제가 됩니다.

```
h1   ← frontmatter title. 컴포넌트가 출력. 마크다운에 쓰지 않음
h2   ← 요약. 한 문장
p    ← 디스크립션 2~3줄. 키워드 포함. description으로 사용
h2   ← 본문 소제목
p
h3   ← 소제목 아래 세부 항목
p
h2   ← 본문 소제목
...
```

**티스토리 등 h1을 자동 점유하는 플랫폼과 다릅니다.** 그런 플랫폼은 본문 소제목을 h3부터 쓸 수밖에 없지만, 본 사이트는 h1이 비어 있으므로 본문 소제목이 h2입니다. 계층 구조는 동일하고 태그 번호만 한 단계 위입니다.

목차는 h2 기준으로 자동 생성하므로, 본문 소제목을 h3로 내리면 목차가 비게 됩니다.

| 태그 | 개수 | 역할 |
|---|---|---|
| h1 | 1 | 글 제목. 컴포넌트가 렌더링 |
| h2 | 6~8 | 요약 1개 + 본문 소제목 |
| h3 | 필요시 | h2 내부 세부 구분 |
| h4 이하 | 0 | 사용하지 않음 |

### 첫 h2 — 요약

제목을 다른 표현으로 바꿔 쓰면서, 이 글이 무엇을 다루는지 알리는 **한 문장**으로 작성합니다.

- h1: `척추관협착증과 허리디스크, 앉을 때와 걸을 때`
- h2: `척추관협착증과 허리디스크, 어떻게 다른지 알아보겠습니다`

제목과 같은 문장을 반복하지 않습니다. 키워드 과다로 읽힐 수 있습니다.

### 디스크립션 — 본문에서 가장 중요한 부분

첫 h2 바로 아래 2~3줄입니다.

- **검색 결과에 노출되는 유일한 본문입니다.** 클릭 여부가 여기서 갈립니다
- 핵심 키워드가 반드시 들어갑니다
- 오늘 무엇을 말하려는지가 명확해야 합니다
- 그대로 frontmatter의 `description`이 됩니다
- 80~120자

이 부분만큼은 여러 번 고쳐 쓰십시오. 본문을 다 쓴 뒤 마지막에 다시 손보는 편이 좋습니다.

### 소제목 작성

**독자와 검색엔진 모두 소제목으로 본문 내용을 파악합니다.** 소제목만 훑어도 글의 흐름이 보여야 합니다.

정보가 담긴 소제목을 씁니다.

| 나쁨 | 좋음 |
|---|---|
| 두 질환은 무엇이 다른가 | 디스크는 눌리는 문제, 협착증은 좁아지는 문제 |
| 어떻게 해야 하나 | 진료받으러 가실 때 이 네 가지를 메모해 가세요 |
| 주의사항 | 이런 증상이면 글을 읽지 말고 병원으로 가세요 |

소제목에 가능한 범위에서 키워드를 자연스럽게 포함시킵니다. 다만 억지로 넣지 않습니다.

### 이미지

| 종류 | 방침 |
|---|---|
| 직접 만든 그래프·표·도해 | 제한 없음. 많을수록 좋음 |
| 무료 스톡 사진 | 사용하지 않음 |
| 타인의 이미지 | 절대 금지 |

정적 사이트에 이미지 최적화가 적용되므로 장수 제한이 필요 없습니다. 다만 의미 없는 장식 사진은 로딩만 늦추고 순위에 도움이 되지 않습니다.

모든 이미지에 `alt` 속성을 답니다.

### 문체

**공손체(~합니다, ~하십니다)를 사용합니다.** 정보체(~다, ~이다)는 쓰지 않습니다.

독자가 40~60대이며, 건강은 조심스럽게 전달해야 하는 주제이기 때문입니다.

- 지시보다 권유: "하세요" → "하시는 편이 좋습니다"
- 단정 금지: "낫습니다" → "도움이 되는 경우가 많습니다"
- 전문 용어는 괄호로 풀이: 유착성 관절낭염(오십견)

### 글 구조 템플릿

1. 상황 제시 + 키워드 (2~3문단, description으로 사용)
2. 이럴 땐 병원으로 (적색 신호) — 위험 신호는 앞쪽에 배치
3. 왜 그런가 — 비유 중심의 쉬운 설명
4. **흔한 오해 하나 바로잡기**
5. 데이터나 연구 (있을 경우)
6. 지금 해볼 수 있는 것 / 피해야 할 것
7. 진료 때 이렇게 전달하세요
8. 정리
9. 참고문헌 + 면책 + 저자

**4번은 모든 글에 반드시 들어갑니다.** 다른 글들이 똑같이 말하는 것 중 사실과 다른 하나를 짚어주는 부분이며, 이 사이트의 차별점입니다.

### 금지 표현

- 완치, 근본 치료, 즉시 효과
- "○일 만에", "이것만 하면"
- 특정 병원·제품·시술 추천
- 진단 확정 표현 ("당신은 ○○입니다")

---

## 11. 발행 순서

`CONTENT-PLAN.md`의 발행 순서를 따릅니다. 목표는 30편이며, 애드센스는 20편 시점에 신청합니다.

토픽 클러스터 구조이므로 **기둥글(pillar)은 가지글이 모인 뒤 마지막에 작성**합니다. 순서를 바꾸지 마십시오.

## 12. 하지 말 것

- 카테고리를 5개에서 늘리거나 줄이기
- 글 20편 미만에서 애드센스 신청
- 다크모드, 검색 기능, 댓글 등 초기 범위 밖 기능 추가
- 면책·저자·적색신호 컴포넌트 생략
- 서버 렌더링에 의존하는 페이지 생성
- 건강 외 주제 콘텐츠 추가
- 카테고리 칩을 클라이언트 사이드 필터로 구현하기
- 글 카드에서 description 생략하기
- 방문자 데이터 없이 "인기 글" 표기하기
- 참고 사이트의 애니메이션·인터랙티브 요소 따라 만들기
- 광고 슬롯을 3곳보다 많이 만들기
